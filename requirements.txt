# Runtime. All permissively licensed (Apache-2.0 / MIT / BSD) so the project
# can stay closed-source. Ultralytics YOLO is deliberately NOT used: it is
# AGPL-3.0, which would force this codebase open or require a paid licence.
numpy>=1.24
opencv-python-headless>=4.8
onnxruntime>=1.16
rtmlib>=0.0.13

# Exercise recognition. Both BSD-3-Clause.
scikit-learn>=1.3
joblib>=1.3
